{
  "name": "@hydra-platform/identity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 1 — handles, key domains, fresh identities. Invariant I1 lives here.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3"
  }
}
