{
  "name": "@hydra-platform/channel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test test/*.test.ts"
  },
  "description": "Phase 2 — the pointer. Invariant I3 lives here.",
  "dependencies": {
    "@scure/starknet": "^2.4.0"
  }
}
