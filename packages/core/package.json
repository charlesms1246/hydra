{
  "name": "@hydra/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Every hydra operation, as plain functions returning plain data. The CLI, the agent surface and the TUI are all views over this."
}
